import { StyleSheet, Text } from 'react-native';

const Clasificacion = ({nombreJugador, intentos}) => {
    return (
      <Text>Estas en clasificacion</Text>
    )
}


const styles = StyleSheet.create({});

export default Clasificacion;